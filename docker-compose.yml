version: '3.8'

services:
  app:
    build: .
    container_name: schooltechshare
    ports:
      - "3000:3000"
    volumes:
      # Persist the SQLite database
      - ./database.sqlite:/usr/src/app/database.sqlite
      # Persist uploaded images
      - ./public/uploads:/usr/src/app/public/uploads
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
